<form method="post" action="{{ path('program_delete', {'programSlug': program.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.id) }}">
	<button class="btn">Delete</button>
</form>
